VITE_TELEMETRY_ENABLED=true
VITE_TELEMETRY_ENDPOINT=/api/telemetry

# PostgreSQL publishing store. Use a pooled connection string in serverless deployments.
DATABASE_URL=
DATABASE_SSL=true

# Private token used by /settings/data/operasi for draft, review, publish and rollback actions.
EDITORIAL_API_TOKEN=
